// lib/constants/app_constants.dart
import 'package:flutter/material.dart';

class AppConstants {
  static const Duration defaultAnimationDuration = Duration(milliseconds: 180);
  static const double standardBorderRadius = 16.0;
  static const double cardBorderRadius = 20.0;
  static const double dialogBorderRadius = 24.0;
  
  static const EdgeInsets standardScreenPadding = EdgeInsets.symmetric(horizontal: 32, vertical: 28);
  static const EdgeInsets dialogContentPadding = EdgeInsets.all(24);
}